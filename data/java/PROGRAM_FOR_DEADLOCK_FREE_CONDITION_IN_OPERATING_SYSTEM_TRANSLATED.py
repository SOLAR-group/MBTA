import sys

def f_gold ( process , need ) :
    import sys
    import os
    import subprocess
    import tempfile
    import pickle
    import tempfile
    import tempfile.tempdir
    import tempfile.tempdir
    import tempfile.tempdir
    import tempfile.tempdir
    import tempfile.tempdir
    import tempfile.tempdir
    import tempfile.tempdir
    class PROGRAM_FOR_DEADLOCK_FREE_CONDITION_IN_OPERATING_SYSTEM :
        def f_gold ( self , process , need ) :
            min_resources = 0
            min_resources = process * ( need - 1 ) + 1
            return min_resources
    return PROGRAM_FOR_DEADLOCK_FREE_CONDITION_IN_OPERATING_SYSTEM

if __name__ == '__main__':
    param = [
    (38,37,),
    (82,3,),
    (2,26,),
    (38,72,),
    (31,85,),
    (80,73,),
    (11,9,),
    (2,31,),
    (26,59,),
    (37,67,)
        ]
    n_success = 0
    print("class,mutant,test_index,result")
    for i, parameters_set in enumerate(param):
        try:
            print("PROGRAM_FOR_DEADLOCK_FREE_CONDITION_IN_OPERATING_SYSTEM," + sys.argv[1] + "," + str(i) + "," + str(f_gold(*parameters_set)))
        except:
            print("PROGRAM_FOR_DEADLOCK_FREE_CONDITION_IN_OPERATING_SYSTEM," + sys.argv[1] + "," + str(i) + ",EXCEPTION")
